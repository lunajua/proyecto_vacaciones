from setuptools import setup, find_packages

setup(
    name='registro-vacaciones',
    version='1.0.0',
    description='Sistema de gestión modular de vacaciones para empleados',
    author='Tato',
    packages=find_packages(),
    install_requires=[],
    python_requires='>=3.8',
)